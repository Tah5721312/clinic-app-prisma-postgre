# نظام إدارة العيادة الطبية

🏥 **Medical Clinic Management System**

نظام شامل لإدارة العيادات الطبية - إدارة المرضى، الأطباء، المواعيد، السجلات الطبية، والفواتير

**Comprehensive Medical Clinic Management System**

---

## نظرة عامة

نظام إدارة عيادة طبية شامل مبني باستخدام **Next.js 15** و **TypeScript** و **PostgreSQL**. يوفر النظام إدارة كاملة لجميع جوانب العيادة الطبية من إدارة المرضى والأطباء إلى المواعيد والسجلات الطبية والفواتير.

---

## المميزات الرئيسية

### 🔐 نظام المصادقة والصلاحيات

- نظام مصادقة متكامل باستخدام **NextAuth.js**
- نظام صلاحيات مرن (RBAC) باستخدام **CASL**
- أدوار متعددة: Super Admin, Admin, Doctor, Patient, Receptionist, Nurse
- صلاحيات دقيقة على مستوى الحقول

### 👥 إدارة المستخدمين

- إدارة شاملة للمرضى (CRUD)
- إدارة الأطباء والتخصصات
- إدارة المستخدمين والصلاحيات
- ملفات شخصية للمستخدمين

### 📅 إدارة المواعيد

- جدولة المواعيد مع نظام الأوقات المتاحة
- إدارة جداول الأطباء الأسبوعية
- أنواع المواعيد: كشف عادي، متابعة، طوارئ
- حالة المواعيد: معلق، مجدول، ملغي
- تقويم تفاعلي لعرض المواعيد

### 📋 السجلات الطبية

- إنشاء وإدارة السجلات الطبية
- ربط السجلات بالمرضى والأطباء
- تسجيل التشخيص، الأعراض، الأدوية، وخطة العلاج
- تسجيل العلامات الحيوية (ضغط الدم، الحرارة، الطول، الوزن)
- صلاحيات محددة: الأطباء يرون سجلاتهم فقط، المرضى يرون سجلاتهم فقط

### 💰 إدارة الفواتير

- إنشاء وإدارة الفواتير
- ربط الفواتير بالمواعيد
- تتبع حالة الدفع (غير مدفوع، جزئي، مدفوع، ملغي)
- حساب الخصومات والمبالغ المتبقية
- تقارير الإيرادات (يومي، شهري، إجمالي)

### 📊 لوحة التحكم (Dashboard)

- إحصائيات شاملة للعيادة
- عدد المرضى، الأطباء، المواعيد
- إحصائيات المواعيد (اليوم، الأسبوع، الشهر)
- إحصائيات الإيرادات
- حالة المواعيد (معلق، مجدول، ملغي)
- عدد الأطباء المتاحين

### 🔍 البحث والفلترة

- بحث متقدم بالاسم في جميع الصفحات
- فلترة حسب التخصص، الطبيب، المريض
- بحث في المواعيد، المرضى، الأطباء

---

## التقنيات المستخدمة

- ⚡️ **Next.js 15** - مع App Router
- ⚛️ **React 18** - واجهة المستخدم
- ✨ **TypeScript** - للكتابة الآمنة
- 💨 **Tailwind CSS 3** - للتصميم
- 🗄️ **PostgreSQL** - قاعدة البيانات
- 🔧 **Prisma ORM** - إدارة قاعدة البيانات
- 🔐 **NextAuth.js** - المصادقة
- 🛡️ **CASL** - إدارة الصلاحيات
- 🎨 **Lucide React** - الأيقونات
- 📱 **Responsive Design** - تصميم متجاوب

---

## متطلبات التشغيل

- **Node.js** 18+
- **pnpm** (مفضل) أو npm
- **PostgreSQL** 12+ أو قاعدة بيانات PostgreSQL مستضافة (مثل Neon, Supabase)

---

## التثبيت والتشغيل

### 1. تثبيت المتطلبات

```bash
# تثبيت الحزم
pnpm install
```

### 2. إعداد متغيرات البيئة

قم بإنشاء ملف `.env` في المجلد الرئيسي:

```env
# Database Configuration
DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"

# NextAuth Configuration
NEXTAUTH_SECRET="your_secret_key_here"
NEXTAUTH_URL="http://localhost:3000"

# Application Domain
NEXT_PUBLIC_DOMAIN="http://localhost:3000"

# Admin Credentials (Optional)
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="admin_password"

# Guest Credentials (Optional)
GUEST_EMAIL="guest@example.com"
GUEST_PASSWORD="guest_password"
```

### 3. إعداد قاعدة البيانات

```bash
# تشغيل migrations لإنشاء الجداول
pnpm prisma migrate deploy

# (اختياري) ملء قاعدة البيانات ببيانات تجريبية
pnpm prisma db seed
```

### 4. تشغيل المشروع

```bash
# وضع التطوير
pnpm dev

# بناء المشروع للإنتاج
pnpm build

# تشغيل الإنتاج
pnpm start
```

افتح [http://localhost:3000](http://localhost:3000) في المتصفح.

---

## هيكل المشروع

```
clinic-app-prisma-postgre/
├── src/
│   ├── app/                    # صفحات Next.js (App Router)
│   │   ├── Dashboard/          # لوحة التحكم
│   │   ├── patients/           # إدارة المرضى
│   │   ├── doctors/            # إدارة الأطباء
│   │   ├── appointments/       # إدارة المواعيد
│   │   ├── medical-records/    # السجلات الطبية
│   │   ├── invoices/           # إدارة الفواتير
│   │   └── api/                # API Routes
│   ├── components/             # المكونات المشتركة
│   ├── lib/                    # المكتبات والمساعدات
│   │   ├── prisma.ts           # Prisma Client
│   │   ├── db_utils.ts         # دوال قاعدة البيانات
│   │   ├── ability.ts          # تعريف الصلاحيات
│   │   └── types.ts            # تعريفات TypeScript
│   ├── hooks/                  # React Hooks
│   └── contexts/               # React Contexts
├── prisma/
│   ├── schema.prisma           # Prisma Schema
│   └── migrations/             # Database Migrations
└── README.md                   # هذا الملف
```

---

## الأدوار والصلاحيات

### Super Admin (211)

- جميع الصلاحيات على النظام

### Admin (212)

- إدارة المرضى، الأطباء، المواعيد
- إدارة السجلات الطبية
- إدارة الفواتير
- عرض لوحة التحكم

### Doctor (213)

- قراءة وتحديث بياناته
- قراءة بيانات المرضى
- إدارة المواعيد
- إنشاء وإدارة السجلات الطبية الخاصة به
- عرض لوحة التحكم

### Patient (216)

- قراءة وتحديث بياناته
- قراءة سجلاته الطبية
- إدارة مواعيده
- عرض الفواتير الخاصة به

### Receptionist (215)

- إدارة المرضى
- إدارة المواعيد
- قراءة بيانات الأطباء

### Nurse (214)

- قراءة بيانات المرضى والأطباء
- قراءة المواعيد

---

## الميزات الإضافية

- 🌙 **Dark Mode** - وضع الظلام
- 🔍 **Global Search** - بحث عام في النظام
- 📱 **Responsive Design** - تصميم متجاوب لجميع الأجهزة
- 🎨 **Modern UI** - واجهة مستخدم عصرية
- ⚡️ **Performance Optimized** - محسّن للأداء
- 🔒 **Secure** - آمن ومحمي
- 📊 **Audit Logs** - سجلات المراجعة

---

## أوامر Prisma المفيدة

```bash
# عرض حالة migrations
pnpm prisma migrate status

# إنشاء migration جديد
pnpm prisma migrate dev --name migration_name

# تطبيق migrations على الإنتاج
pnpm prisma migrate deploy

# فتح Prisma Studio (واجهة قاعدة البيانات)
pnpm prisma studio

# إعادة توليد Prisma Client
pnpm prisma generate
```

---

## المساهمة

نرحب بالمساهمات! يرجى فتح Issue أو Pull Request.

---

## الترخيص

هذا المشروع مخصص للاستخدام الخاص.

---

## الدعم

للحصول على الدعم، يرجى فتح Issue في المستودع.

---

## ملاحظات مهمة

- تأكد من تحديث `DATABASE_URL` في ملف `.env` قبل التشغيل
- استخدم `pnpm prisma migrate deploy` لتطبيق migrations على قاعدة البيانات
- في حالة وجود مشاكل في الـ sequences، تأكد من تطبيق جميع migrations بشكل صحيح
