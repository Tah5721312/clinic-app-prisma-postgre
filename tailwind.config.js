/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class',
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      fontFamily: {
        primary: ['Inter', 'system-ui', 'sans-serif'],
      },
      colors: {
        // الألوان الأساسية - يغير: ألوان الأزرار، الروابط، العناصر الرئيسية، الهوفر
        primary: {
          50: '#f0f9ff',   // خلفيات فاتحة جداً
          100: '#e0f2fe',  // خلفيات فاتحة
          200: '#bae6fd',  // أزرار ثانوية فاتحة
          300: '#7dd3fc',  // حالات الهوفر الفاتحة
          400: '#38bdf8',  // أزرار وأيقونات
          500: '#0ea5e9',  // اللون الأساسي (الأزرار، الروابط)
          600: '#0284c7',  // الأزرار عند الضغط (المعتاد)
          700: '#0369a1',  // الأزرار عند الهوفر
          800: '#075985',  // حدود وتفاصيل
          900: '#0c4a6e',  // حالات مظلمة
          950: '#082f49',  // حالات داكنة جداً
        },
        
        // ألوان الوضع المظلم - يغير: خلفيات الصفحات والأقسام في الوضع المظلم
        dark: {
          50: '#f8fafc',   // خلفيات الصفحات في الوضع المظلم الفاتح جداً
          100: '#f1f5f9',  // خلفيات الأقسام الفاتحة
          200: '#e2e8f0',  // خلفيات البطاقات الفاتحة
          300: '#cbd5e1',  // حدود العناصر
          400: '#94a3b8',  // نصوص ثانوية
          500: '#64748b',  // نصوص أساسية
          600: '#475569',  // خلفيات العناصر
          700: '#334155',  // خلفيات البطاقات والأقسام
          800: '#1e293b',  // خلفيات الصفحات الرئيسية
          900: '#64748b',  // خلفيات داكنة
          950: '#020617',  // خلفيات داكنة جداً (ثابتة)
        },
        
        // ألوان النص - يغير: لون النصوص في الوضع الفاتح والمظلم
        text: {
          light: '#f8fafc',    // نصوص فاتحة (للخلفيات الداكنة)
          muted: '#cbd5e1',    // نصوص خافتة/ثانوية
          dark: '#1e293b',     // نصوص داكنة (للخلفيات الفاتحة)
        },
        
        // ألوان البطاقات - يغير: خلفيات البطاقات في الوضع الفاتح والمظلم
        card: {
          light: '#eae9ea',    // خلفيات البطاقات في الوضع الفاتح
          dark: '#eae9ea',     // خلفيات البطاقات في الوضع المظلم
        }
      },
      animation: {
        flicker: 'flicker 3s linear infinite',
        shimmer: 'shimmer 1.3s linear infinite',
      },
    },
  },
  plugins: [
    require('@tailwindcss/forms'),
  ],
}
